fn main() { 
    // Using a macro rather than a function, denoted by the '!' at the end of "println!"
    // To compile, use "rustc main.rs". To run, use "./main" like you would any other linux executable not in your path 
    println!("Hello, world!");
}